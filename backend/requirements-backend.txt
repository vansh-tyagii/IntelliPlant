# Install this AFTER your existing requirements-platform.txt and PPE/requirements.txt.
# These are only the packages the new agentic backend layer needs that
# weren't already pinned elsewhere in your project.

# fastapi>=0.111
# uvicorn[standard]>=0.29
# pydantic>=2.6
python-dotenv>=1.0
pyyaml>=6.0

# Already required by rag_agent.py / build_db.py - listed here so the
# backend is installable standalone too.
langchain-core>=0.2
langchain-community>=0.2
langchain-text-splitters>=0.2
langchain-groq>=0.1
faiss-cpu>=1.8
sentence-transformers>=2.7

# Already required by module45/fusion_inference.py + fusion_agent.py.
# catboost>=1.2
# shap>=0.45
# pandas>=2.0
# numpy>=1.26

# Already required by swat/src/module1_swat.py (SwatAgent) - not listed in
# requirements-platform.txt, so it must be listed here.
tensorflow>=2.15

# Already required by ai41/src/inference.py (Ai4iAgent) - not listed in
# requirements-platform.txt, so it must be listed here.
# joblib>=1.3
# scikit-learn>=1.4

# Transitive dependency of sentence-transformers (HuggingFaceEmbeddings),
# pinned explicitly so installs don't silently skip or mismatch it.
torch>=2.2
